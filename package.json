{
  "name": "lichen",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run src/build.ts",
    "check": "bun run src/build.ts --check",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "bun": ">=1.4.0"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/bun": "^1.4.0"
  }
}
